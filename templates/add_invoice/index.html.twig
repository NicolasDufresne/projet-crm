{% extends 'base.html.twig' %}

{% block title %}Add Invoice{% endblock %}

{% block body %}
    <h1>Add invoice</h1>

    {{ form_start(invoice) }}
    {{ form_row(invoice.ref) }}
    {{ form_row(invoice.status) }}
    {{ form_row(invoice.TVA) }}
    {{ form_row(invoice.TTC) }}
    {{ form_row(invoice.HT) }}


    <button type="submit" class="btn">Add </button>
    {{ form_end(invoice) }}
{% endblock %}