{% extends 'base.html.twig' %}

{% block title %}Add Ticket{% endblock %}

{% block body %}
    <h1>Add ticket</h1>

    {{ form_start(ticket) }}
    {{ form_row(ticket.object) }}
    {{ form_row(ticket.details) }}
    {{ form_row(ticket.status) }}


    <button type="submit" class="btn">Add </button>
    {{ form_end(ticket) }}
{% endblock %}